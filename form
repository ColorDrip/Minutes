<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Project Idea Submission</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f2f4f8;
      margin: 0;
      padding: 40px 20px;
    }

    .container {
      max-width: 1000px;
      margin: auto;
      background: #fff;
      border-radius: 12px;
      padding: 40px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.06);
    }

    h2 {
      text-align: center;
      color: #333;
      margin-bottom: 10px;
    }

    .note {
      text-align: center;
      font-size: 14px;
      color: #777;
      margin-bottom: 30px;
    }

    .tabs {
      display: flex;
      justify-content: center;
      margin-bottom: 30px;
    }

    .tab-button {
      background-color: #e7eaf0;
      border: none;
      padding: 12px 24px;
      margin: 0 5px;
      border-radius: 8px 8px 0 0;
      font-size: 16px;
      cursor: pointer;
      transition: 0.3s;
    }

    .tab-button.active {
      background-color: #ffffff;
      border-bottom: 3px solid #0078d4;
      color: #0078d4;
      font-weight: bold;
    }

    .tab-content {
      display: none;
    }

    .tab-content.active {
      display: block;
    }

    .form-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 25px;
    }

    .form-group {
      display: flex;
      flex-direction: column;
    }

    label {
      font-size: 14px;
      margin-bottom: 6px;
      color: #444;
      font-weight: 500;
    }

    .required-star {
      color: red;
      margin-left: 3px;
    }

    input[type="text"],
    textarea,
    select {
      padding: 12px;
      font-size: 14px;
      border: 1px solid #ccc;
      border-radius: 8px;
      transition: border 0.3s ease;
    }

    input:focus,
    textarea:focus {
      border-color: #0078d4;
      outline: none;
    }

    textarea {
      resize: vertical;
    }

    .radio-group {
      display: flex;
      flex-wrap: wrap;
      gap: 15px;
      margin-top: 8px;
    }

    .radio-group label {
      font-size: 14px;
      color: #555;
    }

    .full-width {
      grid-column: span 2;
    }

    .btn {
      background-color: #0078D4;
      color: white;
      border: none;
      padding: 14px 24px;
      font-size: 16px;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease;
      display: block;
      margin: 30px auto 0;
    }

    .btn:hover {
      background-color: #005fa3;
    }

    @media (max-width: 768px) {
      .form-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>

<div class="container">
  <h2>Project / Idea Submission</h2>
  <p class="note">Fields marked with <span class="required-star">*</span> are required.</p>

  <!-- Tabs -->
  <div class="tabs">
    <button class="tab-button active" onclick="openTab(event, 'detailsTab')">Details</button>
    <button class="tab-button" onclick="openTab(event, 'approvalTab')">Approval</button>
  </div>

  <form>
    <!-- Details Tab -->
    <div id="detailsTab" class="tab-content active">
      <div class="form-group full-width">
        <label>Project Type<span class="required-star">*</span></label>
        <div class="radio-group">
          <label><input type="radio" name="projectType" value="Automation" required /> Automation</label>
          <label><input type="radio" name="projectType" value="Report" /> Report</label>
          <label><input type="radio" name="projectType" value="Workflow" /> Workflow</label>
          <label><input type="radio" name="projectType" value="Other" /> Other</label>
        </div>
      </div>

      <div class="form-grid">
        <div class="form-group">
          <label>Search Service<span class="required-star">*</span></label>
          <input type="text" required />
        </div>
        <div class="form-group">
          <label>Selected Service<span class="required-star">*</span></label>
          <input type="text" required />
        </div>

        <div class="form-group">
          <label>Region<span class="required-star">*</span></label>
          <input type="text" required />
        </div>
        <div class="form-group">
          <label>Market<span class="required-star">*</span></label>
          <input type="text" required />
        </div>

        <div class="form-group">
          <label>Frequency<span class="required-star">*</span></label>
          <input type="text" required />
        </div>
        <div class="form-group">
          <label>Category<span class="required-star">*</span></label>
          <input type="text" required />
        </div>

        <div class="form-group">
          <label>Current Effort (hrs)<span class="required-star">*</span></label>
          <input type="text" required />
        </div>
        <div class="form-group">
          <label>Monthly Time Saved (hrs)<span class="required-star">*</span></label>
          <input type="text" required />
        </div>

        <div class="form-group">
          <label>Expected Monthly Report Count<span class="required-star">*</span></label>
          <input type="text" required />
        </div>
        <div class="form-group">
          <label>Phase of the Idea<span class="required-star">*</span></label>
          <input type="text" required />
        </div>

        <div class="form-group">
          <label>Expected Completion Date<span class="required-star">*</span></label>
          <input type="text" required />
        </div>
        <div class="form-group">
          <label>Developer Name<span class="required-star">*</span></label>
          <input type="text" required />
        </div>

        <div class="form-group full-width">
          <label>Problem Statement<span class="required-star">*</span></label>
          <textarea rows="3" required></textarea>
        </div>

        <div class="form-group full-width">
          <label>Idea Description<span class="required-star">*</span></label>
          <textarea rows="3" required></textarea>
        </div>
      </div>
    </div>

    <!-- Approval Tab -->
    <div id="approvalTab" class="tab-content">
      <div class="form-grid">
        <div class="form-group">
          <label>Approver Name<span class="required-star">*</span></label>
          <input type="text" required />
        </div>
        <div class="form-group">
          <label>Approval Status<span class="required-star">*</span></label>
          <select required>
            <option value="">Select</option>
            <option value="Approved">Approved</option>
            <option value="Pending">Pending</option>
            <option value="Rejected">Rejected</option>
          </select>
        </div>

        <div class="form-group full-width">
          <label>Approval Comments</label>
          <textarea rows="3"></textarea>
        </div>
      </div>
    </div>

    <button class="btn" type="submit">Submit Idea</button>
  </form>
</div>

<script>
  function openTab(evt, tabId) {
    const tabButtons = document.querySelectorAll(".tab-button");
    const tabContents = document.querySelectorAll(".tab-content");

    tabButtons.forEach(btn => btn.classList.remove("active"));
    tabContents.forEach(tab => tab.classList.remove("active"));

    document.getElementById(tabId).classList.add("active");
    evt.currentTarget.classList.add("active");
  }
</script>

</body>
</html>