Here's a stronger version of your prompt that explicitly instructs the AI to generate a single self-contained HTML file.

Generate a complete, production-quality BPMN Process Designer as one single HTML file only.

Important output requirements:

- Return only the HTML code.
- The response must start with "<!DOCTYPE html>" (or "<html>") and end with "</html>".
- Do not provide explanations, markdown, code fences, or additional text before or after the HTML.
- Everything must be contained in this single HTML file.
- You may use CDN links for libraries such as BPMN.io and any other open-source JavaScript/CSS libraries.
- All custom HTML, CSS, and JavaScript must be embedded inside this single HTML document.
- The generated HTML should be directly savable as "index.html" and run immediately in a browser.

Technology

- HTML5
- CSS3
- Vanilla JavaScript (ES6+)
- BPMN.io (via CDN)
- Any required open-source CDN libraries that improve layout, animation, or user experience.

Objective

Create a professional, enterprise-grade BPMN Process Designer comparable to commercial BPMN modeling tools.

Features

- BPMN 2.0 editor
- Professional modern UI
- Responsive layout
- Drag-and-drop modeling
- Property panel
- Toolbar
- Context menus
- Undo/Redo
- Copy/Paste
- Multi-select
- Search
- Keyboard shortcuts
- Dark/Light mode
- Auto-save
- Import BPMN
- Export BPMN
- Export PNG
- Export SVG
- Export PDF

Smart Layout

Implement intelligent automatic layout:

- No overlapping BPMN objects.
- Maintain proper spacing.
- Auto-align objects.
- Auto-route connectors.
- Keep labels readable.
- Resize pools and lanes automatically.
- Snap-to-grid.
- Alignment guides.
- Auto-organize diagrams after editing.

Canvas

- Infinite canvas.
- Smooth pan and zoom.
- Mouse wheel zoom.
- Mini-map.
- Zoom controls.
- Fit to Screen.
- Fit Width.
- Fit Height.
- Zoom to Selection.
- Center Diagram.

BPMN Support

Support all BPMN 2.0 elements:

- Events
- Tasks
- Gateways
- Pools
- Lanes
- Sub-processes
- Sequence Flows
- Message Flows
- Associations
- Data Objects
- Data Stores
- Groups
- Text Annotations
- Boundary Events
- Intermediate Events
- Event Sub-processes

Simulation

Include an execution simulator:

- Animated tokens moving through sequence flows.
- Highlight active tasks.
- Change color of completed tasks.
- Support multiple tokens.
- Parallel gateway execution.
- Pause.
- Resume.
- Stop.
- Restart.
- Adjustable playback speed.
- Execution statistics.
- Animated flow lines.

User Experience

- Smooth animations.
- Professional icons.
- Modern typography.
- Beautiful color palette.
- Clean spacing.
- Enterprise dashboard appearance.
- Fast performance.
- Handle large diagrams efficiently.

Validation

- BPMN validation.
- Detect disconnected elements.
- Detect missing start/end events.
- Display validation messages.
- Highlight errors visually.

Code Quality

- Well structured.
- Easy to maintain.
- Modular JavaScript inside the HTML.
- Well-commented.
- Optimized performance.
- No duplicate code.
- Clean architecture.

Deliverable

Return exactly one complete HTML document that starts with "<!DOCTYPE html>" (or "<html>") and ends with "</html>". The HTML should be immediately runnable by saving it as "index.html", without requiring any additional files other than external CDN resources. Do not include explanations, markdown, or any text outside the HTML document.This prompt is optimized for "vibe coding" tools and maximizes the chances of getting a single, ready-to-run HTML file that you can save directly as index.html.