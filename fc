

You are a Principal BPMN Software Architect, Senior Front-End Engineer, and Expert BPMN.io Developer with 20+ years of experience building enterprise workflow modeling applications.

Your task is to write a complete, production-ready application as ONE SINGLE HTML FILE.

Critical Instructions

- Output ONLY code.
- Do NOT explain anything.
- Do NOT use Markdown.
- Do NOT wrap the code in triple backticks.
- Begin with "<!DOCTYPE html>".
- End with "</html>".
- The response must contain nothing except the HTML document.
- Embed all CSS and JavaScript inside the HTML.
- You may use CDN libraries (BPMN.io and other open-source libraries).
- The HTML must run immediately when saved as "index.html".

If the code exceeds the model's output limit

Do NOT shorten the code.
Instead:

- Stop exactly where the output limit is reached.
- End the response with the text:

"<!-- CONTINUE PART 2 -->"

When I reply Continue, resume from the exact next line without repeating any previous code.

If another response is needed, end with:

"<!-- CONTINUE PART 3 -->"

Continue until the HTML document is fully completed and finally end with:

"</html>"

Never restart from the beginning.

Build an enterprise-grade BPMN Designer with:

- BPMN.io
- Professional UI
- Modern toolbar
- Property panel
- Minimap
- Infinite canvas
- Fit to Screen
- Zoom controls
- Auto Layout
- No overlapping objects
- Smart connector routing
- Grid snapping
- Import/Export BPMN
- Export PNG/SVG/PDF
- Undo/Redo
- Copy/Paste
- Search
- Multi-select
- Dark/Light mode
- Keyboard shortcuts
- Responsive layout

Simulation

Implement a workflow simulator with:

- Animated execution tokens.
- Token movement along sequence flows.
- Highlight active tasks.
- Parallel execution.
- Pause/Resume/Stop.
- Speed control.
- Execution statistics.
- Animated connectors.

Code Quality

Write clean, modular, production-quality JavaScript with excellent performance and maintainability.

Remember:
Output only the HTML code. Never explain your work. Never summarize. Never omit features. Continue across multiple responses until the single HTML file is complete.One practical note: even with this prompt, ChatGPT and most coding models have a maximum response length. No prompt can force the model to emit an arbitrarily large file in a single response. The "Continue Part 2/3" approach is the most reliable way to generate very large HTML applications.